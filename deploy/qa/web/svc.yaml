kind: Service
apiVersion: v1
metadata:
  name: nest-server
  namespace: backend
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9090
  selector:
    app: nest-server
  type: ClusterIP
